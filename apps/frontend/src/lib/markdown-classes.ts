/**
 * Re-exporta as classes utilitárias de markdown do pacote engine.
 * Mantido como ponte para evitar quebra de resolução de módulo.
 */
export { getMarkdownClasses, markdownStyles } from "@silo/engine/format/markdown";

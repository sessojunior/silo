export interface MonitoringPageDto {
  id: string;
  name: string;
  url?: string;
  imageUrl?: string;
  status?: string;
}

export interface MonitoringRadarDto {
  id: string;
  name: string;
  url: string;
}
